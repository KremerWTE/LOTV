@inherits LayoutComponentBase
@* The fallback layout for a page that names none. It adds nothing around the page: this used to be the Blazor template
   (a "Home / Counter / Weather" sidebar), which showed on every page that draws its own header. *@
@Body
